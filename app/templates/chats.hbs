<div class="col-md-4">
	<div class="list-group">
		{{#each}}
			{{#link-to 'chat' this class='list-group-item'}}
				{{name}}
			{{/link-to}}

		{{/each}}
	</div>
</div>
<div class="col-md-8">
	{{outlet}}
</div>